# Copy to .env for local builds (optional). For production, set secrets in Cloudflare Pages → Settings → Environment variables.

# Cloudflare API (used by wrangler deploy — see DEPLOY.md)
# CLOUDFLARE_API_TOKEN=
